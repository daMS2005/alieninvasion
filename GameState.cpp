#include "GameState.hpp"

// This file can hold any state-related functions or helpers if needed

// Example: Transitioning between states
GameState transitionTo(GameState currentState, GameState nextState) {
    // Handle logic when transitioning between states, if necessary
    // For now, simply return the next state
    return nextState;
}
